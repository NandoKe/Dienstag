/* JS Code, der nur auf der Start Page verwendet wird */

/*var currentUrl = window.location.href;

window.scroll(
    function runIfUrlContains(specificString) {
        if (currentUrl.includes(specificString)) {
            var element = document.getElementById("shop-link");
            element.classList.add("active");
        } else {
            element.classList.remove("active");
        };
    }
);*/

/*var currentUrl = window.location.href;

function runIfUrlContains(specificString) {
    var element = document.getElementById("shop-link");
    if (element) {  // Ensure the element exists
        if (currentUrl.includes(specificString)) {
            element.classList.add("active");
        } else {
            element.classList.remove("active");
        }
    }
}

window.addEventListener("scroll", function() {
    runIfUrlContains("#shop");
});*/